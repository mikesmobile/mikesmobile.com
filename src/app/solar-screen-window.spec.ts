import { SolarScreenWindow } from './solar-screen-window';

describe('SolarScreenWindow', () => {
  it('should create an instance', () => {
    expect(new SolarScreenWindow()).toBeTruthy();
  });
});
